BACKEND:

Get CSV data -- Scrape the same courses with the both scrapers at least an example

Crete a prototype in python (maybe pandas first then with pg db) -- read in the data from the CSV and try some calculations on it.

Modify the minimal_mvc_pg wiht our website logic


FRONTEND:

Create a webpage laout and decide core featueres

Get chat to write some golike CSS to implement it
